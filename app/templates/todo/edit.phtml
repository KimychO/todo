<div class="edit-form">
    <form action="<?php echo $action ?>" method="post">
        <label for="name">Name Of Todo</label>
        <input type="text" name="name" placeholder="Name goes here" value="<?php echo $todo['name'] ?>"/>

        <label for="description">Description of Todo</label>
        <textarea name="description" id="" cols="200" rows="10"
                  placeholder="description"><?php echo $todo['description']; ?></textarea>

        <input type="submit" name="submit" value="Submit">
    </form>
    <div>
        <h2>Points</h2>
        <form data-bind="submit: addPoint">
            Add Point: <input data-bind="value: newPointText" placeholder="What needs to be done?"/>
            <button type="submit">Add</button>
        </form>
        <button data-bind="click: save">Save</button>

        <ul data-bind="foreach: points, visible: points().length > 0">
            <li>
                <input data-bind="value: description"/>
                <a href="#" data-bind="click: $parent.removePoint">Delete</a>
            </li>
        </ul>
    </div>
</div>
<script type="text/javascript">
    ko.applyBindings(new TaskListViewModel(<?php echo $todoId?>));
</script>